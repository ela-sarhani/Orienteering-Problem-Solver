# Shared Test Instance

This folder contains the benchmark instance used across the exact and metaheuristic approaches.

- `example_op.json`: structured Orienteering Problem instance in JSON format

The same test file is used for Branch and Bound, Branch and Cut, Dynamic Programming, Genetic Algorithm, Particle Swarm Optimization, and GRASP so the comparison stays fair.
